#include<stdio.h>
int main()
{
    int size, a[size], i;
    printf("Enter the size of your array: ");
    scanf("%d",&size);
    printf("Enter the array of number: ");
    for(i=0;i<=size;i++)
    {
        scanf("%d",&a[i]);
    }

    printf("%d", a[i]);
return 0;

}
